[package]
name = "hermes-studio"
version = "0.1.0"
description = "A lightweight visual control plane for Hermes Agent"
authors = ["Hermes Studio contributors"]
edition = "2021"
rust-version = "1.97"
license = "MIT"
repository = "https://github.com/kinocode-jp/hermes-studio"

[lib]
name = "hermes_studio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
getrandom = "0.3"
hmac = "0.12"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tauri = { version = "2", features = ["webview-data-url"] }

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
