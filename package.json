{
  "name": "hermes-office",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kinocode-jp/hermes-office.git"
  },
  "homepage": "https://github.com/kinocode-jp/hermes-office#readme",
  "bugs": {
    "url": "https://github.com/kinocode-jp/hermes-office/issues"
  },
  "packageManager": "npm@10.9.8",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names server,web \"npm run dev:server\" \"npm run dev:web\"",
    "dev:web": "npm run dev --workspace @hermes-office/web",
    "dev:server": "HERMES_OFFICE_ALLOWED_ORIGINS=http://localhost:4173,http://127.0.0.1:4173 npm run dev --workspace @hermes-office/server",
    "dev:desktop": "npm run tauri:dev --workspace @hermes-office/desktop",
    "start": "node scripts/start-office.mjs",
    "build:production": "npm run build:web && npm run build --workspace @hermes-office/server",
    "build:desktop-assets": "npm run build:web && npm run bundle:server",
    "bundle:server": "esbuild apps/server/src/index.ts --bundle --platform=node --format=esm --target=node22 --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=apps/desktop/src-tauri/resources/server/hermes-office-server.mjs",
    "build": "npm run build --workspaces --if-present",
    "build:web": "npm run build --workspace @hermes-office/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test:server": "npm test --workspace @hermes-office/server"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "esbuild": "^0.28.1"
  },
  "engines": {
    "node": ">=22 <23",
    "npm": ">=10 <11"
  }
}
